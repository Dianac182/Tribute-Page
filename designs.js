/*
$(function () {

    const myNewHeaders = $('.new-headers');

    $(window).on('resize', function() {
        if ($(window).width() >= 992) {
            myNewHeaders.remove();
        } else {
            myNewHeaders.insertAfter('.my-headers');
        }
    });
});*/
